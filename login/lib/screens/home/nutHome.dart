import 'package:flutter/material.dart';

class NutHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: Text("This is Home"),
    ));
  }
}
